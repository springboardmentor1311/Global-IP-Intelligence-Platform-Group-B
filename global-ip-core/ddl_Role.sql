CREATE TABLE roles
(
    role_id   VARCHAR(255) NOT NULL,
    role_type VARCHAR(255) NOT NULL,
    CONSTRAINT pk_roles PRIMARY KEY (role_id)
);
CREATE TABLE roles
(
    role_id   VARCHAR(255) NOT NULL,
    role_type VARCHAR(255) NOT NULL,
    CONSTRAINT pk_roles PRIMARY KEY (role_id)
);
CREATE TABLE roles
(
    role_id   VARCHAR(255) NOT NULL,
    role_type VARCHAR(255) NOT NULL,
    CONSTRAINT pk_roles PRIMARY KEY (role_id)
);
CREATE TABLE roles
(
    role_id   VARCHAR(255) NOT NULL,
    role_type VARCHAR(255) NOT NULL,
    CONSTRAINT pk_roles PRIMARY KEY (role_id)
);